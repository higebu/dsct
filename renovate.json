{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:best-practices",
    ":semanticCommits"
  ],
  "labels": ["dependencies"],
  "packageRules": [
    {
      "description": ".github/workflows/release.yml is generated by dist and `dist plan` fails when it drifts; its action pins live in dist-workspace.toml under github-action-commits",
      "matchFileNames": [".github/workflows/release.yml"],
      "enabled": false
    },
    {
      "description": "packet-dissector, packet-dissector-core, packet-dissector-pcap, and packet-dissector-test-alloc are published in lockstep from the same repo; bumping only one alone breaks the build because dsct depends on more than one directly",
      "matchPackageNames": ["/^packet-dissector(-core|-pcap|-test-alloc)?$/"],
      "groupName": "packet-dissector"
    }
  ]
}
